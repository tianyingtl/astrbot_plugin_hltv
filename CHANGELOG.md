# Changelog

## v1.4.1 (2026-07-27)

- 修复比分"静默失踪"：对局初期 HLTV 详情页的比分尚未同步（全是 `-`，
  实测确认），此前会什么都不显示让人以为功能坏了，现在明确提示
  "比分暂未同步"；比分抓取失败也会记录日志便于排查

## v1.4.0 (2026-07-27)

- **免 @ 响应**：群里直接发 `/hltv ...` 即可，无需 @ 机器人，也不依赖
  AstrBot 全局 `/` 唤醒前缀（插件自行监听 `/hltv` 开头的消息并分发；
  WebUI 配置 `free_wake` 可关闭）。与 @ 唤醒路径互不冲突、不会重复回复，
  普通聊天不受影响

## v1.3.0 (2026-07-27)

- **`/hltv live <队名>`**：只看指定战队的比赛（带比分）。队名模糊匹配
  （`100t` 命中 "100 Thieves"、`mongolz` 命中 "The MongolZ"），不受星级过滤影响；
  该队不在直播时会查未来 24 小时的安排（几点开赛/是否延迟），都没有才回"没有比赛"

## v1.2.0 (2026-07-27)

- **`/hltv live` 显示比分**：地图比分 + 当前地图实时比分（如 `Wildcard 0:1 MongolZ`、
  `当前 Ancient 4:8`）。列表页比分是前端 websocket 填充的拿不到，
  改从单场详情页取（服务器渲染），限前 4 场、缓存 60 秒
- **修复延迟开赛的比赛凭空消失**：过了预定时间但 HLTV 未标记 live 的比赛
  （延迟很常见）此前会被时间窗口丢弃；现在保留并打 ⏳ 标记，
  `/hltv live` 也会单列"已过开赛时间"一节
- **`/hltv live` 默认只看大赛**（与 today/matches 一致按星级过滤，空则自动回退全部）
- **拼错子指令不再沉默**：如 `hltv new` 会提示"你是想输入 /hltv news 吗？"
  （此前框架对未知子指令不派发任何处理器，用户只看到沉默）

## v1.1.0 (2026-07-27)

发布前经 14-agent 审计 + 全指令仿真测试（真实服务器 HTML/JSON 素材驱动全管线，
翻译走真实接口），审计确认的问题已全部修复：

- 每日推送改由 `initialize()` 启动——`on_astrbot_loaded` 只在进程启动时触发一次，
  WebUI 保存配置走 reload 路径会让推送永久失效
- 战队近期战绩的胜负判定按**服务器原始标记**修正（败方带 `lost` 类、
  胜方无标记；浏览器渲染后的 `won` 类是前端加的，爬虫拿不到）
- 奖杯名在 `span.trophyDescription` 的 title 上而非 img，已修正
- 站内搜索接口的选手键实为驼峰 `nickName`，已修正
- `results` 不做星级过滤（打完的比赛用户要完整结果）；直播比分不再解析
  （服务器端为空，由前端 websocket 填充）

**修复（HLTV 改版适配，浏览器实测 + 真实 DOM 片段离线验证）：**

- **matches 页已被 HLTV 改版，库 0.8.3 选择器全部失效**——这是 live/today/matches
  一直显示"没有比赛"的根因。改为自建解析器，基于新版 data-* 属性
  （match-id/stars/live/unix 时间戳），按配置时区精确计算日期
- **team 页新增 "Valve ranking" 行导致库按位置取值错位**——"平均年龄 67"
  实为 Top30 周数。改为按标签解析，年龄/排名各归其位

**新功能：**

- `/hltv ranking` 默认显示 **Valve VRS 排名**（V社积分，全球），
  `ranking asia|europe|americas`（中文可用）看地区 VRS，`ranking hltv` 看 HLTV 自家排名
- `/hltv news <序号>` 查看新闻详情（正文摘要+原文链接）；新闻标题/详情
  自动**中文翻译**（微软 Edge 免费通道，可配置关闭，失败回退原文）
- **每日定时推送今日赛程**：`/hltv sub` 订阅当前会话，WebUI 配置推送时间
- 战队信息大幅丰富：VRS/HLTV 双排名、阵容带国旗、教练、平均年龄、
  Top30 周数、奖杯列表、近三场战绩（✅/❌ 含比分）
- today/matches 过滤后为空时自动回退显示全部场次（不再只回一句"没有比赛"）
- **today/matches 改为滚动 24 小时窗口**（而非日历日）：欧洲赛事在北京时间
  零点后开打是常态，深夜查"今日赛程"要的就是接下来这一晚
- 修复"置顶/为你推荐"区条目缺赛事名的问题（去重时用正文条目回填）
- 搜索超时修复：多次重试 + 代理轮换 + 库传输层兜底
- 以上比赛管线均已用真实抓取数据做端到端测试（解析→过滤→渲染）

## v1.0.1 (2026-07-26)

修复（多智能体审计确认的问题）：

- **新闻**：featured 头条的字段是 `f_title` 而非 `title`，此前所有头条被静默丢弃；
  同时把常规新闻条数从库默认 2 抬到 10
- **战队/选手信息**：库对解析失败的字段返回哨兵值（`None` / `'None'` / `'0'` / `''`），
  此前会原样打给用户（如"近期奖杯：None"、"世界排名 #0"、"战队：None"），
  现统一归一化为"未知/无/未上榜"
- **赛果**：不再混入库默认的"精选赛果"box（无日期、可能超出天数窗口、与按日列表重复），
  并与 matches 一致按 `min_stars` 过滤
- **直播时效**：比赛列表缓存单独压到 ≤60 秒，已结束的比赛不再在 live/today 里滞留 5 分钟
- **搜索容错**：站内搜索按代理列表逐个尝试（此前只用第一个代理，坏一个就全挂）
- **赛事日期**：`12-3` 这类 D-M 格式易被读反，改为"3月12日"
- **空结果提示**：按实际生效的过滤器（星级/关键词白名单）给出对应提示
- **等待提示**：结果已有缓存时秒回，不再多发一条"正在查询"

## v1.0.0 (2026-07-26)

首个版本：今日赛程、近期大赛（星级过滤 + 关键词白名单）、赛果、战队排名 Top50、
任意战队/选手搜索、赛事与新闻；全指令中文别名；WebUI 可视化配置；
TTL 缓存 + 请求串行化对抗风控。
